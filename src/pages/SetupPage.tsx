export function SetupPage() {
  return <main className="center-page"><section className="setup card"><span className="eyebrow">LaluPay setup</span><h1>Supabase is not configured</h1><p>Copy <code>.env.example</code> to <code>.env.local</code>, then add your Supabase project URL and anon public key. Never place a service-role key in this app.</p><p>Once configured, restart the Vite server.</p></section></main>;
}
